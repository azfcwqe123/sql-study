문제: https://school.programmers.co.kr/learn/courses/30/lessons/151138

---

Oracle

```SQL
SELECT HISTORY_ID,
        CAR_ID,
        TO_CHAR(START_DATE, 'YYYY-MM-DD') AS START_DATE,
        TO_CHAR(END_DATE, 'YYYY-MM-DD') AS END_DATE,
        CASE WHEN END_DATE - START_DATE + 1 >= 30 THEN '장기 대여'
        ELSE '단기 대여'
        END AS RENT_TYPE -- 오라클은 별칭에 " " 따옴표를 붙이거나, 따옴표를 쓰지 않던가 해야함.
                         -- 오라클은 ' '을 문자열로 인식하기 때문에 별칭으로 사용 불가능.
FROM CAR_RENTAL_COMPANY_RENTAL_HISTORY
WHERE TO_CHAR(START_DATE, 'YYYY-MM') LIKE '2022-09%' 
ORDER BY HISTORY_ID DESC

```

---

MySQL

```SQL
SELECT HISTORY_ID,
        CAR_ID,
        DATE_FORMAT(START_DATE, '%Y-%m-%d') AS START_DATE,
        DATE_FORMAT(END_DATE, '%Y-%m-%d') AS END_DATE,
        CASE WHEN DATEDIFF(END_DATE, START_DATE) + 1 >= 30 THEN "장기 대여"
        ELSE "단기 대여"
        END AS "RENT_TYPE"
FROM CAR_RENTAL_COMPANY_RENTAL_HISTORY
WHERE START_DATE LIKE "2022-09-%" -- DATE_FORMAT(START_DATE, '%Y-%m') = "2022-09"
ORDER BY HISTORY_ID DESC

```

---
